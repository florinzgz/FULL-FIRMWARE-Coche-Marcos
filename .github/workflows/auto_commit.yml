name: Firmware Build & Verification

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        environment:
          - esp32-s3-n32r16v
          - esp32-s3-n32r16v-release
          - esp32-s3-n32r16v-touch-debug
          - esp32-s3-n32r16v-no-touch
      fail-fast: false

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Cache PlatformIO
      uses: actions/cache@v4
      with:
        path: |
          ~/.platformio
          ~/.cache/pip
        key: ${{ runner.os }}-pio-${{ hashFiles('platformio.ini') }}
        restore-keys: |
          ${{ runner.os }}-pio-

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install PlatformIO
      run: pip install platformio

    - name: Install platform and packages
      run: |
        pio platform install espressif32@6.12.0
        pio pkg install -e ${{ matrix.environment }}

    - name: Build firmware (${{ matrix.environment }})
      run: |
        pio run -t clean -e ${{ matrix.environment }}
        pio run -e ${{ matrix.environment }}

    - name: Verify sdkconfig was generated (${{ matrix.environment }})
      run: |
        if [ -f ".pio/build/${{ matrix.environment }}/config/sdkconfig.h" ]; then
          echo "✅ sdkconfig.h OK for ${{ matrix.environment }}"
        else
          echo "❌ sdkconfig.h missing for ${{ matrix.environment }}"
          exit 1
        fi

    - name: Upload firmware artifact
      uses: actions/upload-artifact@v4
      with:
        name: firmware-${{ matrix.environment }}
        path: .pio/build/${{ matrix.environment }}/firmware.bin
        if-no-files-found: error

  verify:
    runs-on: ubuntu-latest
    needs: build

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts

    - name: Verify build outputs
      run: |
        echo "🔍 Verificación de compilación de firmware"
        echo "=========================================="

        EXPECTED_ENVS=("esp32-s3-n32r16v" "esp32-s3-n32r16v-release" "esp32-s3-n32r16v-touch-debug" "esp32-s3-n32r16v-no-touch")
        TOTAL_ENVIRONMENTS=${#EXPECTED_ENVS[@]}

        BUILD_COUNT=$(find artifacts -name "firmware.bin" | wc -l)
        echo "✅ Compilaciones exitosas: $BUILD_COUNT/$TOTAL_ENVIRONMENTS"

        echo ""
        echo "📦 Artefactos generados:"
        find artifacts -name "firmware.bin" -exec ls -lh {} \;

        if [ "$BUILD_COUNT" -eq "$TOTAL_ENVIRONMENTS" ]; then
          echo ""
          echo "✅ Todos los entornos compilaron correctamente"
        else
          echo ""
          echo "❌ Algunos entornos fallaron la compilación"
          exit 1
        fi

    - name: Generate verification report
      run: |
        EXPECTED_ENVS=("esp32-s3-n32r16v" "esp32-s3-n32r16v-release" "esp32-s3-n32r16v-touch-debug" "esp32-s3-n32r16v-no-touch")

        echo "# 🔧 Firmware Verification Report" > VERIFICATION_REPORT.md
        echo "" >> VERIFICATION_REPORT.md
        echo "**Fecha:** $(date -u '+%Y-%m-%d %H:%M:%S UTC')" >> VERIFICATION_REPORT.md
        echo "**Commit:** ${{ github.sha }}" >> VERIFICATION_REPORT.md
        echo "**Branch:** ${{ github.ref_name }}" >> VERIFICATION_REPORT.md
        echo "" >> VERIFICATION_REPORT.md
        echo "## 📊 Resultados de Compilación" >> VERIFICATION_REPORT.md
        echo "" >> VERIFICATION_REPORT.md
        echo "| Entorno | Estado | Tamaño |" >> VERIFICATION_REPORT.md
        echo "|---------|--------|--------|" >> VERIFICATION_REPORT.md

        for env in "${EXPECTED_ENVS[@]}"; do
          if [ -f "artifacts/firmware-$env/firmware.bin" ]; then
            SIZE=$(ls -lh "artifacts/firmware-$env/firmware.bin" | awk '{print $5}')
            echo "| $env | ✅ OK | $SIZE |" >> VERIFICATION_REPORT.md
          else
            echo "| $env | ❌ FAIL | - |" >> VERIFICATION_REPORT.md
          fi
        done

        echo "" >> VERIFICATION_REPORT.md
        echo "## ✅ Verificaciones Pasadas" >> VERIFICATION_REPORT.md
        echo "" >> VERIFICATION_REPORT.md
        echo "- [x] Todas las dependencias instaladas correctamente" >> VERIFICATION_REPORT.md
        echo "- [x] Código compila sin errores" >> VERIFICATION_REPORT.md
        echo "- [x] Artefactos de firmware generados" >> VERIFICATION_REPORT.md
        echo "- [x] Todos los entornos (dev, release, touch, no-touch) verificados" >> VERIFICATION_REPORT.md
        echo "" >> VERIFICATION_REPORT.md
        echo "---" >> VERIFICATION_REPORT.md
        echo "*Generado automáticamente por GitHub Actions*" >> VERIFICATION_REPORT.md

    - name: Upload verification report
      uses: actions/upload-artifact@v4
      with:
        name: verification-report
        path: VERIFICATION_REPORT.md
